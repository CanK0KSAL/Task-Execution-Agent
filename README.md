# Task Execution Agent

Take-home project scaffold. Setup and usage will be documented in a later phase.

## Setup (placeholder)

```bash
uv sync
uv run python main.py
```

Dev tools and tests: `uv sync --extra dev` then `uv run pytest`.
